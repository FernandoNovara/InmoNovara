@model IEnumerable<InmoNovara.Models.Usuario>

@{
    ViewData["Title"] = "Usuario";
}

<h1>Inicio @ViewData["Title"]</h1>

<p>
    <a asp-action="Create">Crear Nuevo Usuario</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apellido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RolNombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RolNombre)
            </td>
            <td>
            <td>
                <a class="btn btn-warning" asp-action="Editar" asp-route-id="@item.IdUsuario" title="Editar"><i class="fas fa-pencil-alt"></i></a>
                <a class="btn btn-danger" asp-action="Eliminar" asp-route-id="@item.IdUsuario" title="Eliminar"><i class="fas fa-trash-alt"></i></a>
                <a class="btn btn-info" asp-action="Detalles" asp-route-id="@item.IdUsuario" title="Detalles"><i class="fas fa-book"></i></a>

            </td>
        </tr>
}
    </tbody>
</table>
